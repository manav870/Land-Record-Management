{
  "name": "land-record-management",
  "version": "1.0.0",
  "description": "Blockchain-based Land Record Management System",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run --network localhost scripts/deploy.js",
    "node": "hardhat node"
  },
  "keywords": [
    "blockchain",
    "ethereum",
    "land-records",
    "smart-contracts"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.3.1"
  }
}

